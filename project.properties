############################################################
#
# project settings
#
############################################################

project.name=Library
project.path=.
project.index=LibraryTests.html

as3Library=${basedir}

project.name=Library
project.path=.
project.main=LibraryTestRunner.as
project.mainTest=LibraryTestRunner.as
project.deploy.url=

project.testing.server=
project.testing.deploy=
project.testing.docroot=
project.testing.url=

project.staging.server=
project.staging.deploy=
project.staging.docroot=
project.staging.url=

project.live.server=
project.live.deploy=
project.live.docroot=
project.live.url=


############################################################
#
# everything below here shouldn't need to be edited
#
############################################################

src=${basedir}/${project.path}/src
tests=${basedir}/${project.path}/tests
deploy=${basedir}/${project.path}/deploy

as3Library.src=${as3Library}/src
as3Library.tests=${as3Library}/tests
as3Library.docs=${as3Library}/docs
as3Library.experiments=${as3Library}/experiments
	
mx=${as3Library}/mx
core=${as3Library}/core/playerglobal.swc
docs=${as3Library}/docs